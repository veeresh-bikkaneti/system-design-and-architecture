{
  "name": "system-design-mastery-cert",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply system-design-mastery-cert --local",
    "db:migrate:remote": "wrangler d1 migrations apply system-design-mastery-cert --remote"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260910.1",
    "typescript": "~6.0.2",
    "wrangler": "^4.131.0"
  }
}
