import type { OkfCard } from './types.ts';

/**
 * Open Knowledge Format bundle for System Design Mastery.
 * Each concept is one card: a type, a title, and a short body an SLM can
 * actually hold. Bodies are distilled from the course lessons, not a dump
 * of the full MDX. Live lessons:
 * https://veeresh-bikkaneti.github.io/system-design-and-architecture/lesson/<id>/
 */
const COURSE = "https://veeresh-bikkaneti.github.io/system-design-and-architecture/lesson";

export function lessonUrl(id: string): string {
  return `${COURSE}/${id}/`;
}

export const OKF_CARDS: OkfCard[] = [
  {
    id: "okf",
    type: "Guide",
    order: 0,
    title: "Open Knowledge Format (OKF)",
    tags: ["okf", "frontmatter", "markdown", "context", "knowledge"],
    summary: "Vendor-neutral markdown plus YAML frontmatter for agent context.",
    body: "Open Knowledge Format (OKF) is a vendor-neutral way to store the context an agent needs: a folder of markdown files with YAML frontmatter. The only required field is a non-empty type. Title, description, tags, and links are recommended. There is no SDK, account, or database. A human can read a file; a model can be given just the few cards that match a question. This tutor's coursework lives as OKF cards (type Lesson or Guide). Retrieval picks two or three cards so the small model sees a clean slice, not all 36 lessons.",
  },
  {
    id: "langchain-tool-calling",
    type: "Guide",
    order: 0,
    title: "LangChain tool calling",
    tags: ["langchain", "langgraph", "tool", "bind_tools", "react", "agent"],
    summary: "Tools are named functions with a JSON schema. The runtime executes them.",
    body: "LangChain tool calling is a contract, not a cloud. A tool has a name, a description, and a JSON schema. Chat models that support it expose bind_tools(); the reply carries tool_calls with a name and arguments. The application — not the model — runs the function and sends the result back as an observation. Models without native tools use the same loop via a prompt (ReAct): the model writes an action, the runtime parses it, runs the tool, and continues. create_tool_calling_agent works with any model that implements that contract. Tool outputs must be treated as data, not as new instructions, or a page of retrieved text can hijack the agent. This tutor binds two tools: search_lessons and read_concept.",
  },
  {
    id: "tutor-loop",
    type: "Guide",
    order: 0,
    title: "How this tutor runs",
    tags: ["smollm", "local", "browser", "langgraph", "github pages", "wasm"],
    summary: "A LangGraph-shaped loop with a local SmolLM2 model and no API key.",
    body: "The course site is static and can live on GitHub Pages. This tutor does not call OpenAI, Anthropic, or Workers AI. Each question runs the same shape as the course's LangGraph: triage, then retrieve, then reason, then answer. triage keeps the question on the course. retrieve calls search_lessons over the OKF bundle (BM25, on device). read_concept opens the top cards and wraps them as data. When the lessons miss, or the student asks for a source, web_search reads a Wikipedia intro in the browser and the reply cites that article URL. SmolLM2-135M does not choose tools and does not browse by itself. The page runs the tools, then the model may rephrase a lesson answer. Weights download once from Hugging Face and stay in the browser cache.",
  },
  {
    id: "scaling-web-service",
    type: "Lesson",
    order: 1,
    title: "From Zero to Millions: Scaling a Web Service",
    tags: ["scaling", "load balancer", "replica", "cdn", "shard", "vertical", "horizontal"],
    summary: "From one server to millions of users.",
    body: "Start with one server that does everything. Vertical scaling buys a bigger box until the box is the limit. Horizontal scaling adds more boxes behind a load balancer. Split reads onto replicas and leave writes on the primary. Put static files on a CDN so origin is not serving images. Cache hot rows. When one database cannot hold the data, shard it. Do the napkin math first: requests per second, payload size, and storage growth decide which step you actually need. Lesson analogy: a recipe book, photocopies for reads, and the master book for writes.",
  },
  {
    id: "solid-principles",
    type: "Lesson",
    order: 2,
    title: "SOLID: Object-Oriented Design That Ages Well",
    tags: ["solid", "srp", "ocp", "lsp", "isp", "dip", "oop"],
    summary: "Five principles so code survives change.",
    body: "SOLID, told as a home kitchen. Single responsibility: one job per class, like one station that only plates. Open/closed: add a new dish by extending, not by rewriting the stove. Liskov substitution: a substitute must keep the promise of what it replaces. Interface segregation: a small menu beats one giant interface nobody fully uses. Dependency inversion: depend on an abstraction (the recipe) not a concrete brand of oven. The point is change locality — a new requirement should touch a small surface.",
  },
  {
    id: "mvc-to-react",
    type: "Lesson",
    order: 3,
    title: "From MVC to React",
    tags: ["mvc", "mvp", "mvvm", "react", "spa", "frontend"],
    summary: "How UI architecture moved from MVC to components.",
    body: "Smalltalk split a screen into Model (the data), View (what you see), and Controller (input). MVP and MVVM moved more logic out of the controller so views stay thin. Single-page apps stopped reloading the whole page and kept UI state in the browser. React's model is a tree of components: UI is a function of state, and you re-render the tree instead of poking the DOM. The diner analogy: the menu (model), the plate (view), and the waiter (controller) used to be separate jobs; components are booths that hold their own order.",
  },
  {
    id: "cap-theorem",
    type: "Lesson",
    order: 4,
    title: "The CAP Theorem",
    tags: ["cap", "consistency", "availability", "partition", "pacelc"],
    summary: "During a partition you pick consistency or availability.",
    body: "CAP (Gilbert and Lynch): when a network partition splits a distributed store, you cannot have both linearizable consistency and full availability. Consistency here means every read sees the latest committed write. Availability means every request to a non-crashed node gets a response. Partition tolerance is not optional on a real network — packets get lost — so the real choice during a partition is CP (refuse some requests to stay consistent, like ZooKeeper) or AP (answer anyway and reconcile later, like Dynamo-style stores). When the network is healthy, PACELC adds the other trade-off: latency versus consistency. CAP is not 'pick two' in normal operation.",
  },
  {
    id: "load-balancers",
    type: "Lesson",
    order: 5,
    title: "Load Balancers",
    tags: ["load balancer", "l4", "l7", "round robin", "least connections", "sticky"],
    summary: "How traffic is spread across servers.",
    body: "A load balancer sits in front of a fleet and picks a target for each request. Layer 4 balances TCP connections and is fast but blind to HTTP. Layer 7 understands paths, headers, and cookies, so it can route /api to one pool and /static to another. Common algorithms: round robin, least connections, and weighted picks for uneven boxes. Sticky sessions pin a user to one node — convenient, and a trap if that node dies. Health checks pull dead nodes out. The balancer itself must be redundant or it becomes the single point of failure.",
  },
  {
    id: "nosql-databases",
    type: "Lesson",
    order: 6,
    title: "NoSQL in Plain English",
    tags: ["nosql", "key-value", "document", "wide-column", "graph", "sql"],
    summary: "Four NoSQL families and when relational still wins.",
    body: "Pick the data shape, not a trend. Key-value (Redis, Dynamo) is a giant hash map: great for sessions and caches, poor for joins. Document (MongoDB) stores JSON-like records that vary by row — good when each aggregate is read together. Wide-column (Cassandra) handles huge write volumes and time-series if the query matches the partition key. Graph stores excel when the question is about relationships, not rows. Relational is still the default when you need transactions and flexible queries. Many systems use more than one: the system of record in Postgres, a cache in Redis, a graph for recommendations.",
  },
  {
    id: "data-partitioning",
    type: "Lesson",
    order: 7,
    title: "Data Partitioning (Sharding)",
    tags: ["partition", "shard", "hot key", "reshard"],
    summary: "Split a dataset so no node holds all of it.",
    body: "Partitioning splits rows across machines so no single node stores or serves everything. A partition key decides the home of a row. Good keys spread load; a hot key (one celebrity user) melts one shard while others idle. Cross-shard queries and transactions get expensive, so you partition along a boundary the app already uses, such as user id. Resharding moves data and is an operational project, not a flag. This lesson is the introduction; sharding-strategies goes deeper on hash, range, and directory schemes.",
  },
  {
    id: "url-shortener",
    type: "Lesson",
    order: 8,
    title: "Design a URL Shortener",
    tags: ["url shortener", "base62", "hash", "cache", "interview"],
    summary: "Hashing, caching, and rate limits on an interview classic.",
    body: "A URL shortener maps a short key to a long URL. Generate a unique id (database sequence or a hash of the URL) and encode it in base62 so the key is short. Reads dominate writes by orders of magnitude, so cache the mapping at the edge and in Redis. The redirect path must be fast and highly available; the create path can be slower. Estimate storage: keys are tiny, but you still plan for billions of rows and a retention policy. Rate-limit creates so one client cannot fill the keyspace. Analytics (click counts) should be async so the redirect never waits on a write.",
  },
  {
    id: "idempotency",
    type: "Lesson",
    order: 9,
    title: "Idempotency: Making Retries Safe",
    tags: ["idempotency", "retry", "idempotency key", "exactly once"],
    summary: "Retries must not double-charge.",
    body: "Networks fail, so clients retry. A retry of a payment can charge the card twice if the server treats it as a new request. An idempotency key, sent by the client and stored with the first result, makes a duplicate request return the original outcome instead of doing the work again. The key must be unique per logical operation, stored before side effects, and kept at least as long as clients might retry. Some operations are naturally idempotent (setting a value); others (append, charge) need the key. Exactly-once is usually at-least-once delivery plus idempotent handling.",
  },
  {
    id: "authn-authz",
    type: "Lesson",
    order: 10,
    title: "AuthN and AuthZ",
    tags: ["auth", "authentication", "authorization", "jwt", "oauth", "session", "password"],
    summary: "Who you are, and what you may do.",
    body: "Authentication (AuthN) proves identity: passwords (stored hashed, never raw), sessions, or federated login. Authorization (AuthZ) decides what that identity may do: roles, attributes, or relationship-based checks, enforced on the server every time. OAuth2 is delegated authorization — a user lets one app act on their behalf at another service — and OpenID Connect adds identity on top. JWTs carry claims and a signature so a service can trust them without a session lookup, but they are hard to revoke before expiry, so keep them short and put real permission checks in data you can change. Never authorize from a client-supplied role alone.",
  },
  {
    id: "caching-strategies",
    type: "Lesson",
    order: 11,
    title: "Caching Strategies",
    tags: ["cache", "cache-aside", "write-through", "write-behind", "ttl", "stampede"],
    summary: "How data gets into a cache and how it goes stale.",
    body: "Cache-aside: the app reads the cache, and on a miss loads the database and fills the cache. Simple, and the cache can be empty after a flush. Read-through: the cache itself loads the database on a miss. Write-through: writes go to cache and database together, so reads are fresh and writes are slower. Write-behind: write to cache and flush to the database later — fast, and you can lose data if the cache dies. Always set a TTL. A stampede is many requests missing the same key at once and all hitting the database; a lock or early refresh prevents it. Cache invalidation is the hard part: delete or update the key when the source changes, and accept that stale reads happen.",
  },
  {
    id: "cdns",
    type: "Lesson",
    order: 12,
    title: "Content Delivery Networks",
    tags: ["cdn", "edge", "pop", "ttl", "purge", "origin"],
    summary: "Put bytes near the user.",
    body: "A CDN caches content at points of presence close to users. A request hits the nearest edge. On a hit, the origin never sees it. On a miss, the edge fetches origin (often via an origin shield so one miss does not fan out) and stores the response for a TTL. Purging or versioned URLs handle updates; long TTLs on hashed filenames are safe. CDNs shine for static assets and media. Dynamic HTML is harder: you cache only what is safe, or you use the edge to terminate TLS and still reach origin for personalized data. The origin must survive when the CDN cache is cold.",
  },
  {
    id: "cloud-native-architecture",
    type: "Lesson",
    order: 13,
    title: "Cloud-Native Architecture",
    tags: ["cloud", "kubernetes", "serverless", "12-factor", "region", "az"],
    summary: "Building on someone else's computers.",
    body: "A region is a geographic area; availability zones are separate datacenters inside it. Spread across zones so one building does not take you down. Managed services trade control for less ops; self-hosting is the reverse. Kubernetes schedules containers, restarts them, and rolls them out — it does not design your boundaries. Serverless scales to zero and charges per use, with cold starts when a function has been idle. The 12-factor rules (config in the environment, stateless processes, disposability) exist so an instance can die without a ceremony. Read the bill: egress, cross-AZ traffic, and idle GPUs are where cloud designs go broke.",
  },
  {
    id: "message-queues",
    type: "Lesson",
    order: 14,
    title: "Message Queues",
    tags: ["queue", "kafka", "rabbitmq", "sqs", "backpressure", "delivery"],
    summary: "Decouple producers from consumers.",
    body: "A queue lets a producer finish without waiting for the consumer. That absorbs spikes and lets each side scale alone. Delivery guarantees: at-most-once (may drop), at-least-once (may duplicate — pair with idempotency), exactly-once (rare, usually the combination of the two). Ordering is per partition or per queue, not global, once you scale. Backpressure is what happens when consumers fall behind: bound the queue or you will OOM. Kafka is a durable log you can replay. RabbitMQ is a classic broker with routing. SQS is a managed queue with fewer knobs. Use a queue when the work can complete a moment later; do not hide a synchronous user click behind one and pretend it is instant.",
  },
  {
    id: "api-gateways",
    type: "Lesson",
    order: 15,
    title: "API Gateways",
    tags: ["api gateway", "reverse proxy", "routing", "rate limit"],
    summary: "The front door in front of many services.",
    body: "An API gateway is the front door: TLS, routing to the right service, authentication, rate limits, and request shaping live in one place so each service does not reimplement them. A reverse proxy (nginx) forwards HTTP and can load-balance; a gateway adds API-aware policy — keys, quotas, body size, version routing. The risk is a god gateway that holds business logic and becomes a bottleneck and a deploy coupling. Keep policy at the door and domain rules in the services. The gateway is on the critical path, so it needs its own redundancy and a timeout shorter than the user will wait.",
  },
  {
    id: "bff-backend-for-frontend",
    type: "Lesson",
    order: 16,
    title: "BFF: One API Per Client",
    tags: ["bff", "backend for frontend", "mobile", "aggregation"],
    summary: "A backend shaped for one kind of client.",
    body: "One generic API makes every client suffer. Mobile wants a small payload and few round trips; the web app wants a richer graph; partners want a stable public contract. A backend-for-frontend is an API owned by one client type that aggregates downstream services into the shape that client needs. The BFF can change with the UI without breaking other clients. The pitfall is sprawl: a BFF per screen, duplicated business rules, and a distributed monolith of thin wrappers. Keep domain rules in the services. The BFF orchestrates and shapes. If two BFFs copy the same rule, the rule is in the wrong layer.",
  },
  {
    id: "replication-consistency-models",
    type: "Lesson",
    order: 17,
    title: "Replication and Consistency Models",
    tags: ["replication", "quorum", "leader", "eventual", "linearizability", "async"],
    summary: "How copies stay in sync, and what clients observe.",
    body: "Replication copies data so you survive a dead node and serve more reads. Synchronous replication waits for copies before acknowledging — safer, slower, and a slow replica stalls writes. Asynchronous replication acknowledges early and can lose the last writes on failover. Leader-based systems send writes to one primary. Leaderless systems (Dynamo-style) write to several replicas and use quorums: if W + R > N, a read overlaps a write and sees it. Consistency models are what a client actually observes: linearizability (looks like one copy), causal, or eventual (replicas converge if writes stop). Pick the model the product can explain to a user, not the strongest name in a paper.",
  },
  {
    id: "rate-limiting",
    type: "Lesson",
    order: 18,
    title: "Rate Limiting",
    tags: ["rate limit", "token bucket", "leaky bucket", "sliding window", "redis"],
    summary: "Stop one client from ruining the day.",
    body: "Rate limiting caps how fast a client, key, or IP can call you. Token bucket: tokens drip in at a fixed rate, each request spends one, and a burst is allowed up to the bucket size. Leaky bucket smooths output to a constant rate. Fixed windows are easy and unfair at the boundary (two bursts back to back). Sliding windows count the recent interval more honestly. In a fleet, limits must be shared — usually Redis — or each node enforces 1/N of the cap and the math lies. Return 429 with a retry-after. Limit the expensive route, not only the front door, and have a separate, tighter limit for login and write paths.",
  },
  {
    id: "microservices-vs-monolith",
    type: "Lesson",
    order: 19,
    title: "Microservices vs Monolith",
    tags: ["monolith", "microservice", "modular monolith", "distributed monolith"],
    summary: "When splitting services pays off.",
    body: "A monolith is one deployable unit. It is simpler to debug, transact, and refactor until the team and the scaling needs outgrow it. Microservices split that unit so teams deploy independently and scale the hot part. The cost is the network: latency, partial failure, and distributed debugging. A distributed monolith is the failure mode — many services that must deploy together and share a database. A modular monolith (one deploy, hard module boundaries) is the usual right first step. Split a service out when you have a real reason: a team boundary, a scaling hotspot, or a release cadence. Not because a diagram looked mature.",
  },
  {
    id: "microservices-patterns-pitfalls",
    type: "Lesson",
    order: 20,
    title: "Microservices Patterns and Pitfalls",
    tags: ["distributed monolith", "shared database", "temporal coupling", "chatty"],
    summary: "How microservices turn back into one system.",
    body: "The test of a microservice is independent deployability. If shipping a one-line change needs three repos and a lockstep release, you have a distributed monolith. Chatty calls multiply latency: a user request that fans out to twenty services spends its budget on network, not work. A shared database is the cardinal sin — schemas become a hidden API and teams block each other. Temporal coupling means service A is useless while B is down; give A a timeout, a fallback, or an async handoff. Start with a modular monolith and extract the module that has earned a process boundary. Patterns (gateway, BFF, saga, outbox) are tools for the split you already justified.",
  },
  {
    id: "resilience-patterns",
    type: "Lesson",
    order: 21,
    title: "Resilience Patterns",
    tags: ["timeout", "retry", "backoff", "circuit breaker", "bulkhead", "jitter"],
    summary: "Stop one slow dependency from taking everything down.",
    body: "Every remote call needs a timeout shorter than the caller's patience. Retries without backoff amplify an outage: add exponential backoff and jitter so clients do not retry in lockstep. Retry only idempotent calls, or use an idempotency key. A circuit breaker opens after repeated failures and fails fast until a trial succeeds, giving the dependency time to recover. Bulkheads isolate pools (threads, connections) so one noisy neighbor cannot exhaust them all. Graceful degradation returns a smaller answer — cached, partial, or a clear error — instead of hanging. Timeouts, retries, and breakers stack: retry inside the timeout budget, and stop retrying when the breaker is open.",
  },
  {
    id: "stream-vs-batch",
    type: "Lesson",
    order: 22,
    title: "Stream vs Batch",
    tags: ["stream", "batch", "lambda", "kappa", "kafka"],
    summary: "Processing data that never stops, or data that can wait.",
    body: "Batch jobs read a bounded set (yesterday's files) and are simpler to recompute: run them again. Streams process unbounded events as they arrive, which you want for fraud, feeds, and low-latency metrics. Lambda architecture runs both a batch path (accurate, slow) and a speed path (fresh, approximate) and merges them — powerful and expensive to keep correct. Kappa keeps only the stream and recomputes by replaying the log. Choose batch when the user can wait and correctness is a rerun away. Choose stream when the value of the event decays in seconds. Many products do both: a stream for the live counter, a batch job that repairs it overnight.",
  },
  {
    id: "consensus-raft",
    type: "Lesson",
    order: 23,
    title: "Consensus with Raft",
    tags: ["raft", "consensus", "leader election", "quorum", "log"],
    summary: "How unreliable machines agree on one log.",
    body: "Raft gets a cluster to agree on a replicated log. Nodes are followers, candidates, or a single leader. The leader handles writes and replicates log entries. An entry is committed once a majority has stored it, so a minority can die and the committed prefix survives. If the leader vanishes, followers elect a new one using randomized timeouts and terms — a term is a logical era, and stale leaders from old terms are ignored. Split votes are why timeouts are random. Clients talk to the leader. Raft does not make a slow network fast; it makes 'which write happened' unambiguous. etcd and many control planes use it. You need an odd number of nodes so a majority is a clear minority failure away.",
  },
  {
    id: "sharding-strategies",
    type: "Lesson",
    order: 24,
    title: "Sharding Strategies",
    tags: ["sharding", "consistent hashing", "range", "directory", "virtual node"],
    summary: "Hash, range, and directory splits, and the cost of moving.",
    body: "Key-based (hash) sharding spreads rows evenly and makes range queries painful. Range sharding keeps ranges together (user ids 1–1M on shard A) which helps scans and creates hot shards when the latest range is the busy one. Directory-based sharding looks the location up in a map you can edit — flexible, and the directory is now a dependency. Consistent hashing with virtual nodes lets you add a machine and move only a fraction of the keys, not rehash the world. The true cost of resharding is the move itself: dual writes or a backfill, a way to read both places during the migration, and a plan for the hot key you cannot split. Pick the key from the query pattern, then live with it.",
  },
  {
    id: "distributed-transactions",
    type: "Lesson",
    order: 25,
    title: "Distributed Transactions",
    tags: ["2pc", "saga", "outbox", "transaction", "atomicity"],
    summary: "Atomicity across machines, and why it hurts.",
    body: "Two-phase commit has a coordinator ask everyone to prepare, then commit. If the coordinator dies after prepare, participants block holding locks. That is why many teams refuse 2PC across services. A saga is a sequence of local transactions with a compensating action for each step (refund if the next step fails). Sagas do not isolate the way a database transaction does — other readers can see the intermediate state — so you design for that. The outbox pattern writes the business row and an event row in the same local transaction, then a publisher relays the event. That avoids the dual-write bug where the database commits and the message broker never hears about it. Use a single-database transaction while you still can.",
  },
  {
    id: "observability",
    type: "Lesson",
    order: 26,
    title: "Observability for Distributed Systems",
    tags: ["metrics", "logs", "traces", "sli", "slo", "red", "use", "cardinality"],
    summary: "Metrics, logs, and traces that answer a real question.",
    body: "Logs are events you can read. Metrics are numbers you can graph and alert on. Traces follow one request across services. You need all three: a metric says error rate jumped, a trace says which hop, a log says why. RED (rate, errors, duration) fits request-driven services. USE (utilization, saturation, errors) fits resources. Sampling keeps trace cost down; sample errors more aggressively than successes. Cardinality explosions happen when you put user ids in metric labels — the time series database melts. SLI is the measurement the user feels (availability, latency). SLO is the target. The error budget is how much failure the SLO allows before you stop shipping features and fix reliability.",
  },
  {
    id: "domain-driven-design",
    type: "Lesson",
    order: 27,
    title: "Domain-Driven Design",
    tags: ["ddd", "bounded context", "aggregate", "ubiquitous language", "domain event", "outbox"],
    summary: "Model the business the way the business talks.",
    body: "Ubiquitous language means the code uses the same words as the business — an Order is not randomly also a Purchase. A bounded context is a boundary where a word has one meaning; 'customer' in billing is not 'customer' in support. An aggregate is a cluster of objects you change as one transaction, with a root that guards the rules. Domain events say what happened (InvoiceIssued), not what to do. Event storming is a workshop that finds those events on a wall of sticky notes before you draw services. The transactional outbox publishes those events reliably. Event sourcing is the optional next step, covered in its own lesson. Start with language and boundaries, not with a framework.",
  },
  {
    id: "anti-corruption-layer",
    type: "Lesson",
    order: 28,
    title: "Anti-Corruption Layer",
    tags: ["acl", "anti-corruption", "adapter", "strangler", "legacy"],
    summary: "A translation boundary so a messy outside world stays outside.",
    body: "An anti-corruption layer translates between your model and someone else's, so their names, enums, and mistakes do not leak into your domain. It is an adapter plus a translator: their payload comes in, your types go out. Use it against a legacy API, a partner, or a vendor you do not control. The strangler-fig migration puts that layer in front of the old system and moves one behavior at a time behind it until the legacy can be deleted. Without the layer, the new code slowly starts speaking the old dialect and the rewrite never finishes. The layer has a cost — keep it at the edge, not between two modules you own.",
  },
  {
    id: "design-instagram",
    type: "Lesson",
    order: 29,
    title: "Design Instagram",
    tags: ["instagram", "feed", "fan-out", "cdn", "upload", "news feed"],
    summary: "Uploads, metadata, CDNs, and the feed fan-out.",
    body: "Photo upload is a pipeline: the client sends bytes to object storage (not through your app server if you can help it), you store metadata in a sharded database, and workers build thumbnails. Reads of the image itself go through a CDN. The hard part is the feed. Fan-out on write pushes a new post into each follower's timeline at upload time — fast reads, painful when someone has fifty million followers. Fan-out on read builds the timeline at request time — cheap writes, slow reads for people who follow thousands. Large systems hybrid: fan-out on write for normal users, fan-out on read for celebrities. Ranked feeds add a generation step and a cache. Plan for the celebrity, not the average.",
  },
  {
    id: "designing-ai-systems",
    type: "Lesson",
    order: 30,
    title: "Designing AI Systems",
    tags: ["rag", "agent", "inference", "vector", "kv-cache", "quantization", "eval", "fine-tune"],
    summary: "RAG, agents, and the cost of inference.",
    body: "A request flows from prompt to tokens. The model does not know your private docs unless you put them in the prompt. RAG retrieves relevant chunks and stuffs them into the context; it is the right default when facts change often. Fine-tuning changes weights and is for style or a stable skill, not for yesterday's prices. An agent loop lets the model call tools, observe results, and continue, with a max step count so it cannot spin. Vector databases serve similarity search for RAG; they do not replace a source of truth. Inference cost is dominated by memory and decoding: batch requests, reuse the KV-cache, quantize weights, and cache repeated prompts. Evals — a fixed set of questions with expected answers — replace vanity dashboards. This tutor is a small RAG-style loop: OKF cards in, a local model out.",
  },
  {
    id: "agentic-ai-patterns",
    type: "Lesson",
    order: 31,
    title: "Agentic AI Patterns",
    tags: ["agent", "multi-agent", "orchestration", "prompt injection", "tool"],
    summary: "Why multi-agent demos fail in production.",
    body: "An agent is a loop: model, tool call, observation, repeat, then a final answer. Orchestration patterns include a single agent with tools, a router that picks a specialist, and a planner plus workers. Multi-agent demos look magical and fail in production because errors cascade, retries loop, context and bills explode, and a tool result can smuggle prompt injection ('ignore your rules'). Guardrails: a step budget, a dollar budget, tools with narrow schemas, tool output treated as untrusted data, and an eval set that includes hostile inputs. Prefer one well-tooled agent over a committee until you can name the failure the second agent prevents. The course chatbot uses one loop and two tools on purpose.",
  },
  {
    id: "event-sourcing-cqrs",
    type: "Lesson",
    order: 32,
    title: "Event Sourcing and CQRS",
    tags: ["event sourcing", "cqrs", "projection", "snapshot", "append-only"],
    summary: "Store the history, project the present.",
    body: "Event sourcing stores an append-only log of facts (ItemAdded, ItemRemoved) as the source of truth, and derives current state by replay. You get an audit trail and the ability to rebuild a new view later. Snapshots checkpoint state so replay does not read the entire history every time. Projections are read models built from the log. CQRS splits the write model from the read model so each can be shaped for its job — and they will be briefly inconsistent. Ordering, versioning of events, and the cost of replay decide whether the pattern earns its keep. Do not event-source a CRUD form because it sounded advanced. Do it when the history is the product.",
  },
  {
    id: "designing-chat-at-scale",
    type: "Lesson",
    order: 33,
    title: "Designing Chat at Scale",
    tags: ["chat", "websocket", "presence", "fan-out", "e2ee", "ordering"],
    summary: "Messaging like WhatsApp: connections, order, receipts.",
    body: "Chat needs a long-lived connection (WebSocket or similar) because polling does not scale to typing indicators. A connection is sticky to a gateway; a session service maps user to gateway so other services can push. Messages in one conversation must be ordered — a per-conversation sequence or partition, not a global clock. Delivery receipts are states (sent, delivered, read) written reliably, not just inferred from the socket. Presence is ephemeral and lossy; do not put it in the message database. Group chat fan-out is the same celebrity problem as a feed. End-to-end encryption means the server stores ciphertext and cannot index, search, or moderate content it cannot read. Plan that loss before you promise search.",
  },
  {
    id: "graphql-vs-rest",
    type: "Lesson",
    order: 34,
    title: "GraphQL vs REST",
    tags: ["graphql", "rest", "overfetch", "n+1", "caching", "versioning"],
    summary: "Fixed resources versus a query language.",
    body: "REST exposes resources at URLs. Clients share caches, CDNs, and a simple model, and they often over-fetch or under-fetch because the shape is fixed. Versioning is usually in the URL or the media type. GraphQL gives one endpoint and lets the client name the fields it wants, which fits varied clients, and it moves complexity to the server: N+1 queries, expensive nested resolvers, and queries that are denial-of-service cannons unless you cap depth and cost. HTTP caching is harder because every body can differ. REST is the default for a public, cacheable API. GraphQL earns its place when many clients need different slices and you will invest in query limits, dataloaders, and persisted queries.",
  },
  {
    id: "multi-region-active-active",
    type: "Lesson",
    order: 35,
    title: "Multi-Region Active-Active",
    tags: ["multi-region", "rpo", "rto", "active-active", "failover", "conflict"],
    summary: "Surviving a whole region going dark.",
    body: "RPO is how much data you can bear to lose, measured in time. RTO is how long you can bear to be down. Active-passive keeps a warm spare and fails over — simpler, and the spare can be stale by the replication lag. Active-active serves writes in more than one region, which means concurrent writes can conflict and you need a rule (last-write-wins, CRDTs, or a single writer per key). DNS failover is slow and caches lie; do not pretend TTL is a failover mechanism by itself. Data borders (residency laws) can forbid a copy leaving a country. Test the plan by failing a region on purpose. A design you have never failed is a story, not a control.",
  },
  {
    id: "zero-trust-security",
    type: "Lesson",
    order: 36,
    title: "Zero Trust",
    tags: ["zero trust", "mtls", "workload identity", "authenticate", "authorize"],
    summary: "Never trust the network. Verify every request.",
    body: "Castle-and-moat security trusted everything inside the VPN. That fails once a laptop or a dependency is compromised, because lateral movement is free. Zero trust authenticates and authorizes every request, regardless of which network it came from. Workload identity gives each service a cryptographic identity, and mutual TLS proves both ends of a call. Policies are explicit (this service may call that method), not implied by an IP range. The cost: identity issuance, certificate rotation, and a control plane that is now on the critical path. If the front desk that mints identities is down, calls fail closed — plan for that. Zero trust does not replace AuthN and AuthZ; it refuses to skip them just because the caller is 'inside'.",
  },
];
